# agent/os/windows/requirements.txt — Windows-specific agent dependencies
# Install alongside the base agent requirements:
#   pip install -r agent/requirements.txt
#   pip install -r agent/os/windows/requirements.txt

# Windows Service management (SCM, event log, DPAPI)
# After install: python Scripts/pywin32_postinstall.py -install
pywin32>=306

# Credential Manager / DPAPI key storage (optional — falls back to plain file)
keyring>=24.0.0

# System metrics — psutil is in agent/requirements.txt but listed here
# explicitly because the Windows collectors rely heavily on Windows-specific
# psutil APIs (win_service_iter, num_handles, sensors_battery)
psutil>=5.9.8

# Hardened HTTPS transport used by win_agent.py (tls_transport.py)
# requests is already a transitive dependency of many packages but listed
# explicitly here because we depend on HTTPAdapter internals for SPKI pinning.
requests>=2.31.0
urllib3>=2.0.0
